// Code generated by moshi-kotlin-codegen. Do not edit.
@file:Suppress("DEPRECATION", "unused", "UNUSED_PARAMETER", "ClassName", "REDUNDANT_PROJECTION",
    "RedundantExplicitType", "LocalVariableName", "RedundantVisibilityModifier",
    "PLATFORM_CLASS_MAPPED_TO_KOTLIN", "IMPLICIT_NOTHING_TYPE_ARGUMENT_IN_RETURN_POSITION")

package com.sipus.core.`data`.remote.dto

import com.squareup.moshi.JsonAdapter
import com.squareup.moshi.JsonReader
import com.squareup.moshi.JsonWriter
import com.squareup.moshi.Moshi
import com.squareup.moshi.`internal`.Util
import java.lang.NullPointerException
import java.lang.reflect.Constructor
import kotlin.Int
import kotlin.String
import kotlin.Suppress
import kotlin.collections.emptySet
import kotlin.jvm.Volatile
import kotlin.text.buildString

public class DashboardDtoJsonAdapter(
  moshi: Moshi,
) : JsonAdapter<DashboardDto>() {
  private val options: JsonReader.Options = JsonReader.Options.of("total_buku", "total_dipinjam",
      "total_riwayat", "total_denda")

  private val intAdapter: JsonAdapter<Int> = moshi.adapter(Int::class.java, emptySet(), "totalBuku")

  @Volatile
  private var constructorRef: Constructor<DashboardDto>? = null

  override fun toString(): String = buildString(34) {
      append("GeneratedJsonAdapter(").append("DashboardDto").append(')') }

  override fun fromJson(reader: JsonReader): DashboardDto {
    var totalBuku: Int? = 0
    var totalDipinjam: Int? = 0
    var totalRiwayat: Int? = 0
    var totalDenda: Int? = 0
    var mask0 = -1
    reader.beginObject()
    while (reader.hasNext()) {
      when (reader.selectName(options)) {
        0 -> {
          totalBuku = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("totalBuku",
              "total_buku", reader)
          // $mask = $mask and (1 shl 0).inv()
          mask0 = mask0 and 0xfffffffe.toInt()
        }
        1 -> {
          totalDipinjam = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("totalDipinjam",
              "total_dipinjam", reader)
          // $mask = $mask and (1 shl 1).inv()
          mask0 = mask0 and 0xfffffffd.toInt()
        }
        2 -> {
          totalRiwayat = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("totalRiwayat",
              "total_riwayat", reader)
          // $mask = $mask and (1 shl 2).inv()
          mask0 = mask0 and 0xfffffffb.toInt()
        }
        3 -> {
          totalDenda = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("totalDenda",
              "total_denda", reader)
          // $mask = $mask and (1 shl 3).inv()
          mask0 = mask0 and 0xfffffff7.toInt()
        }
        -1 -> {
          // Unknown name, skip it.
          reader.skipName()
          reader.skipValue()
        }
      }
    }
    reader.endObject()
    if (mask0 == 0xfffffff0.toInt()) {
      // All parameters with defaults are set, invoke the constructor directly
      return  DashboardDto(
          totalBuku = totalBuku as Int,
          totalDipinjam = totalDipinjam as Int,
          totalRiwayat = totalRiwayat as Int,
          totalDenda = totalDenda as Int
      )
    } else {
      // Reflectively invoke the synthetic defaults constructor
      @Suppress("UNCHECKED_CAST")
      val localConstructor: Constructor<DashboardDto> = this.constructorRef ?:
          DashboardDto::class.java.getDeclaredConstructor(Int::class.javaPrimitiveType,
          Int::class.javaPrimitiveType, Int::class.javaPrimitiveType, Int::class.javaPrimitiveType,
          Int::class.javaPrimitiveType, Util.DEFAULT_CONSTRUCTOR_MARKER).also {
          this.constructorRef = it }
      return localConstructor.newInstance(
          totalBuku,
          totalDipinjam,
          totalRiwayat,
          totalDenda,
          mask0,
          /* DefaultConstructorMarker */ null
      )
    }
  }

  override fun toJson(writer: JsonWriter, value_: DashboardDto?) {
    if (value_ == null) {
      throw NullPointerException("value_ was null! Wrap in .nullSafe() to write nullable values.")
    }
    writer.beginObject()
    writer.name("total_buku")
    intAdapter.toJson(writer, value_.totalBuku)
    writer.name("total_dipinjam")
    intAdapter.toJson(writer, value_.totalDipinjam)
    writer.name("total_riwayat")
    intAdapter.toJson(writer, value_.totalRiwayat)
    writer.name("total_denda")
    intAdapter.toJson(writer, value_.totalDenda)
    writer.endObject()
  }
}
